apiVersion: v1
kind: Service
metadata:
  name: express-service
  labels:
    app: express
spec:
  type: LoadBalancer  # ← Changed from ClusterIP!
  ports:
  - port: 80          # ← Standard HTTP port
    targetPort: 3000  # ← Your Express app port
    protocol: TCP
  selector:
    app: express