apiVersion: v1
kind: Service
metadata:
  name: hello-service
spec:
  selector:
    app: hello  # This will select pods with label "app: hello"
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP